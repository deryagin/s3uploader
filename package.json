{
  "name": "s3uploader",
  "version": "0.0.1",
  "description": "Демон, выполняющий слежение за появлением новых файлов в ФС и отправляющий их на s3-сервер.",
  "main": "./index.js",
  "scripts": {
    "debug": "node-debug --save-live-edit=true --web-host=`hostname` index.js",
    "test": "./node_modules/mocha/bin/mocha --require=./global.js --require=./loader.js --timeout=100"
  },
  "dependencies": {
    "chokidar": "^1.0.1",
    "knox": "^0.9.2",
    "tasks-queue": "0.0.2"
  },
  "devDependencies": {
    "mocha": "^2.2.5",
    "chai": "^3.0.0",
    "sinon": "^1.15.4",
    "fixture-stdout": "^0.2.1"
  }
}
